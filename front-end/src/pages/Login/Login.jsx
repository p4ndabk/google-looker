import { LoginCard } from "./components/LoginCard";

export const Login = () => {
  return (
    <div className="bg-gradient-energy h-full min-h-screen flex items-center justify-center animate-fade">
      <LoginCard />
    </div>
  );
};
